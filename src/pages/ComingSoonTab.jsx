export default function ComingSoonTab({ label }) {
  return (
    <section className="vd-panel glass-panel">
      <h2>{label}</h2>
      <p className="vd-coming-soon">
        This module is coming in the next build phase.
      </p>
    </section>
  );
}
